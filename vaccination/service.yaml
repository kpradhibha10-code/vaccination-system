apiVersion: v1
kind: Service
metadata:
  name: vaccination-service
spec:
  type: NodePort
  selector:
    app: vaccination
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007